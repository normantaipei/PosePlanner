# PosePlanner 找圖前端（Nuxt）— 連線設定
# 複製成 .env 後填入你自己的值。.env 不會進版控。
#
# 這些是「server 端私有」變數：Nuxt 用它們在 server route（/api/*）代理你的私有 DB
# server，token 永遠不會送到瀏覽器。Nuxt 會自動把 NUXT_ 前綴對應到 runtimeConfig。

# 你的私有 DB server domain（對應 runtimeConfig.poseplannerBaseUrl）
NUXT_POSEPLANNER_BASE_URL=http://192.168.x.x:8000

# 『讀取』token（server 的 POSEPLANNER_READ_TOKEN）；server 沒設 token 就留空。
NUXT_POSEPLANNER_TOKEN=
